{
  "name": "mertakca.github.io",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:content": "node scripts/render-content.mjs",
    "dev": "npm run generate:content && vite",
    "build": "npm run generate:content && tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "check": "npm run lint && npm run build"
  },
  "dependencies": {
    "htmx.org": "^2.0.7"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.2",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.62.0",
    "vite": "^8.1.1"
  }
}
